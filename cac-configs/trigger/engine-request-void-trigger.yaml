kind: trigger
name: engine-request-void-trigger
metadata:
  title: Engine request.void 触发器
spec:
  sourceInterceptor: engine-webhook-filter
  targetInterceptor: never-intercept
  binding: engine-webhook-binding
  template: engine-request-void-template
  targetSystem: engine-webhook-api
